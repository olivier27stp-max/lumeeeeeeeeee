// DEPRECATED — Replaced by PresetSelectModal.tsx
// This stub exists only so old import paths don't break during transition.

import React from 'react';

interface TemplateSelectModalProps {
  isOpen: boolean;
  isFr: boolean;
  onSelectTemplate: (template: any) => void;
  onStartFromScratch: () => void;
  onClose: () => void;
}

export default function TemplateSelectModal(_props: TemplateSelectModalProps) {
  return null;
}

node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# ── Environment & secrets ──
.env*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
credentials.json
service-account*.json

# ── Security ──
*.sqlite
*.db
.secret*

# ── Guard: never commit scripts that embed Supabase keys ──
# qa/test scripts must load credentials from env vars only.
scripts/*-with-keys.*
scripts/local-*.env
scripts/.env*

# ── Playwright (E2E web smoke tests) ──
test-results/
playwright-report/
blob-report/
.playwright/

# ── IDE ──
.idea/
.vscode/settings.json
*.swp
*.swo

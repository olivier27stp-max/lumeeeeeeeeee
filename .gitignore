node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# ── Environment & secrets ──
.env*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
credentials.json
service-account*.json

# ── Security ──
*.sqlite
*.db
.secret*

# ── Guard: never commit scripts that embed Supabase keys ──
# qa/test scripts must load credentials from env vars only.
scripts/*-with-keys.*
scripts/local-*.env
scripts/.env*

# ── IDE ──
.idea/
.vscode/settings.json
*.swp
*.swo

# Claude Code personal local settings
.claude/settings.local.json

# Email inbox — local mockups/preview (not committed)
plan-email-preview.html
maquette-email-messages.html

# Sorties du robot de recette — régénérables (npm run qa:carte)
qa-carte.json
qa-carte-boutons.json
qa-rapport.md
qa-captures/
